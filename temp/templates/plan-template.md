# 实施方案：[功能名称]

**分支**：`[###-功能名称]` | **日期**：[日期] | **规格文档**：[链接]
**输入**：来自 `/specs/[###-功能名称]/spec.md` 的功能规格说明

**说明**：本模板由 `/speckit.plan` 命令填充。执行工作流请参见 `.specify/templates/commands/plan.md`。

## 概述

[从功能规格中提取：核心需求 + 调研得出的技术方案]

## 技术背景

<!--
  需执行操作：将本节内容替换为项目的技术细节
  此处结构仅作参考，用于指导迭代过程。
-->

**语言/版本**：[例如：Python 3.11、Swift 5.9、Rust 1.75 或 待确认]  
**核心依赖**：[例如：FastAPI、UIKit、LLVM 或 待确认]  
**存储方案**：[如适用，例如：PostgreSQL、CoreData、文件 或 不适用]  
**测试框架**：[例如：pytest、XCTest、cargo test 或 待确认]  
**目标平台**：[例如：Linux 服务器、iOS 15+、WASM 或 待确认]
**项目类型**：[单项目/网页/移动端 - 决定源码结构]  
**性能目标**：[领域相关，例如：1000 请求/秒、1 万行/秒、60 帧/秒 或 待确认]  
**约束条件**：[领域相关，例如：p95 延迟 <200 毫秒、内存占用 <100MB、支持离线使用 或 待确认]  
**规模/范围**：[领域相关，例如：1 万用户、100 万行代码、50 个页面 或 待确认]

## 合规性检查

*关卡：必须在 0 阶段调研前通过。1 阶段设计完成后重新检查。*

[基于合规性文件确定的检查项]

## 项目结构

### 文档（本功能）

```text
specs/[###-功能名称]/
├── plan.md              # 本文件（/speckit.plan 命令输出）
├── research.md          # 0 阶段输出（/speckit.plan 命令）
├── data-model.md        # 1 阶段输出（/speckit.plan 命令）
├── quickstart.md        # 1 阶段输出（/speckit.plan 命令）
├── contracts/           # 1 阶段输出（/speckit.plan 命令）
└── tasks.md             # 2 阶段输出（/speckit.tasks 命令 - 非 /speckit.plan 创建）
```

### 源代码（代码仓库根目录）
<!--
  需执行操作：将下方占位符目录结构替换为本功能的具体布局。
  删除未使用的选项，并将选定结构扩展为实际路径（例如：apps/admin、packages/something）。
  最终交付的方案中不得包含“选项”标签。
-->

```text
# [如未使用则删除] 选项 1：单项目（默认）
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [如未使用则删除] 选项 2：网页应用（检测到“前端”+“后端”时使用）
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [如未使用则删除] 选项 3：移动端 + API（检测到“iOS/Android”时使用）
api/
└── [与上述后端结构一致]

ios/ 或 android/
└── [平台特定结构：功能模块、UI 流程、平台测试]
```

**结构决策**：[记录选定的结构，并引用上述实际目录]

## 复杂度跟踪

> **仅当合规性检查存在需说明理由的违规项时填写**

| 违规项 | 必要性说明 | 舍弃更简单替代方案的原因 |
|-----------|------------|-------------------------------------|
| [例如：第 4 个项目] | [当前需求] | [为何 3 个项目不足以满足需求] |
| [例如：仓储模式] | [具体问题] | [为何直接数据库访问无法满足需求] |

---