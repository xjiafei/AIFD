# CLAUDE.md - AI全流程研发项目指南

> 本文档是 Claude Code 在本项目中工作的核心指导文档，定义了AI驱动的全自动化软件研发流程。

## 📋 目录

- [核心原则](#核心原则)
- [项目说明](#项目说明)
- [技术栈](#技术栈)
- [目录结构](#目录结构)
- [开发规范](#开发规范)
- [工作流程](#工作流程)
- [Skills使用指南](#skills使用指南)
- [MCP集成说明](#mcp集成说明)
- [质量保证](#质量保证)
- [关键说明](#关键说明)
- [快速开始示例](#快速开始示例)

---

## 🎯 核心原则

### 1. 自动化优先

**原则说明**：
- AI 主导执行 80% 以上的开发工作，包括代码生成、测试、文档编写
- 凡是能通过脚本、代码生成或 MCP 工具完成的任务，严禁向用户索取手动操作
- 主动执行 Shell 命令、文件读写和 Git 操作，不等待人工指令

**实践要点**：
- 不要等待人工指令完成每一步，而是自主完成整个流程
- 在关键决策点（PRD审核、技术方案审核、生产部署）主动输出待审核文档，等待人工确认
- 遇到技术问题时，先尝试在知识库中搜索解决方案，然后自动修复

### 2. 契约驱动

**原则说明**：
- 后端与前端的交互必须严格遵循 OpenAPI 3.0 规范（`docs/tech/api-spec.yaml`）
- 任何接口变更必须先更新契约文档，再同步代码
- 契约作为前后端协作的单一真实来源

**契约流程**：
```
ArchitectAgent 生成 api-spec.yaml
    ↓
BackendAgent 根据契约生成 Controller/DTO
    ↓
FrontendAgent 根据契约生成 API 调用函数
    ↓
TestAgent 根据契约生成 API 测试用例
```

### 3. 质量内建

**原则说明**：
- 代码生成时即考虑质量，而非事后修补
- 每个环节都有自动化的质量检查
- 测试驱动，先有测试用例再写代码

**质量关卡**：
```
代码生成 → Lint检查 → 单元测试 → 集成测试 → E2E测试 → 部署验证
```

**自检机制**：
- 代码生成后必须进行自检（静态检查、单元测试）
- 测试未通过视为开发未完成，需自动进入"修复-重测"循环
- 最多重试3次，失败后请求人工介入

### 4. 文档先行

**原则说明**：
- 先设计文档，再写代码
- 所有决策和设计都要有文档记录
- 每次交付后总结经验教训到知识库

**文档类型**：
- **需求文档**：PRD、用户故事
- **设计文档**：架构设计、API设计（OpenAPI）、数据库设计
- **测试文档**：测试计划、测试用例
- **运维文档**：部署手册、运维手册
- **知识文档**：经验总结、最佳实践

### 5. 显式知识

**原则说明**：
- 遇到不确定的业务逻辑或技术细节，优先查询 `knowledge/` 目录下的知识库
- 若知识库无解，则必须向用户提问，严禁臆测
- 持续积累开发经验，形成可复用的知识库

**知识管理**：
- 每次交付后更新知识库
- 提取可复用代码片段
- 记录常见问题和解决方案
- 总结最佳实践

### 6. 增量交付

**原则说明**：
- 将大需求拆解为小的可交付单元
- 每个单元都能独立测试和部署
- 快速验证，快速反馈

---

## 📖 项目说明

### 项目概述

本项目是一个 **AI驱动的全流程软件研发系统**，目标是实现从需求输入到系统交付的全自动化流程。

### 核心能力

1. **需求理解**：自动分析用户需求，生成结构化的产品需求文档（PRD）
2. **技术设计**：自动进行架构设计、数据库设计、API设计（OpenAPI 3.0）
3. **代码生成**：自动生成后端代码、前端代码和测试代码
4. **自动化测试**：自动执行单元测试、集成测试、E2E测试
5. **自动化部署**：自动生成部署配置，执行部署流程
6. **知识积累**：持续积累开发经验，形成可复用的知识库

### 架构模式

**编排者-执行者模式**：

```
用户需求输入
    ↓
主控 Agent（Claude Code）
    ↓
协调 SubAgents：
├── ProductAgent     → 需求分析 + PRD生成
├── ArchitectAgent   → 技术设计 + API契约
├── BackendAgent     → 后端代码开发
├── FrontendAgent    → 前端代码开发
├── TestAgent        → 测试用例 + 自动化测试
└── DevOpsAgent      → 部署配置 + 自动化部署
    ↓
需求交付完成
```

### 项目特点

- **多仓库管理**：后端、前端、测试各自独立的 Git 仓库
- **契约驱动**：OpenAPI 3.0 作为前后端协作的核心契约
- **前后端分离**：Vue 3 前端 + Spring Boot 后端
- **全栈测试**：单元测试、集成测试、E2E测试全覆盖
- **容器化部署**：Docker + Docker Compose 部署方案
- **自愈机制**：测试失败自动修复，最多重试3次

### Baseline 和 Feature-Specs 说明

#### 核心概念

本项目采用 **Baseline（基线）+ Feature-Specs（功能规格）** 的双层文档管理模式：

**Baseline（基线规格）**：
- **位置**：`docs/specs/baseline/`
- **定义**：项目的全量规格文档，包含所有已实现功能的最新设计
- **内容**：
  - 项目整体架构（architecture.md）
  - 完整API规格（api-spec.yaml，包含所有已实现的API）
  - 完整数据库设计（database.md，包含所有已创建的表）
  - 完整前端设计（frontend.md，包含所有已实现的页面/组件）
  - 测试规格（test-plan.md, test-cases.md）
- **作用**：是理解项目全貌和现有系统的**唯一真实来源**

**Feature-Specs（功能规格）**：
- **位置**：`docs/specs/feature{N}-specs/`
- **定义**：单个特性的增量规格文档，每个新特性一个独立文件夹
- **内容**：
  - 该特性的产品设计（product/prd/prd.md）
  - 该特性的技术设计（tech/architecture.md, api-spec.yaml, database.md, frontend.md）
  - 该特性的测试设计（test/test-plan.md, test-cases.md）
- **作用**：隔离单个特性的设计，便于版本管理和变更追踪

#### 工作流程

```
阶段1：新特性启动
用户需求 → 创建 feature{N}-specs/ 目录

阶段2：理解现有系统
↓
阅读 baseline/ → 理解现有架构、API、数据库、前端
（重要：必须先理解 baseline，才能设计增量）

阶段3：设计新特性
↓
在 feature{N}-specs/ 中设计增量部分
- 基于 baseline 的理解
- 只设计新增/变更的部分
- 不重复 baseline 的内容

阶段4：实现和测试
↓
根据 feature{N}-specs/ 生成代码 → 自动化测试

阶段5：合并到 baseline
↓
特性交付后 → 将 feature{N}-specs/ 合并到 baseline/
- 更新 baseline/tech/api-spec.yaml（添加新API）
- 更新 baseline/tech/database.md（添加新表/字段）
- 更新 baseline/tech/architecture.md（添加新模块）
- 更新 baseline/tech/frontend.md（添加新页面/组件）
↓
Baseline 始终保持最新，反映当前系统完整状态
```

#### 关键原则

1. **Baseline 是唯一真实来源**
   - 需要了解系统全貌时，查看 baseline
   - 新成员加入项目时，从 baseline 开始学习
   - 代码必须与 baseline 保持一致

2. **设计新特性前必须先读 baseline**
   - 理解现有架构和技术选型
   - 了解已实现的功能和API
   - 确保新设计与现有系统兼容
   - 避免重复设计已有功能

3. **Feature-specs 是增量设计**
   - 只包含该特性的新增/变更部分
   - 设计时引用 baseline，但不修改 baseline
   - 作为独立文档便于版本管理

4. **交付后必须合并到 baseline**
   - Feature 交付完成后立即合并
   - 保持 baseline 始终最新
   - Feature-specs 保留作为历史记录

#### 示例

**示例1：设计第一个特性"用户登录"**
```bash
# 1. 创建 feature-specs 目录
/create-spec 用户登录
→ 创建 docs/specs/feature1-specs/

# 2. 生成 PRD
/senior-pm 用户登录
→ 输出到 docs/specs/feature1-specs/product/prd/prd.md

# 3. 阅读 baseline（如果是首个特性，baseline可能为空）
cat docs/specs/baseline/tech/api-spec.yaml
cat docs/specs/baseline/tech/database.md

# 4. 生成技术设计
/tech-design 用户登录
→ 基于 baseline 理解，设计登录API、用户表等
→ 输出到 docs/specs/feature1-specs/tech/

# 5. 实现代码
/implement 用户登录

# 6. 交付后合并到 baseline
→ 将 feature1-specs/tech/api-spec.yaml 合并到 baseline/tech/api-spec.yaml
→ 将 feature1-specs/tech/database.md 合并到 baseline/tech/database.md
→ baseline/ 现在包含"用户登录"功能的完整设计
```

**示例2：设计第二个特性"订单管理"（依赖"用户登录"）**
```bash
# 1. 创建 feature-specs 目录
/create-spec 订单管理
→ 创建 docs/specs/feature2-specs/

# 2. 生成 PRD
/senior-pm 订单管理

# 3. 阅读 baseline（此时已包含"用户登录"功能）
cat docs/specs/baseline/tech/api-spec.yaml
→ 可以看到已有的登录API、用户认证机制
cat docs/specs/baseline/tech/database.md
→ 可以看到已有的用户表

# 4. 生成技术设计
/tech-design 订单管理
→ 基于 baseline 理解，设计订单API、订单表
→ 复用 baseline 中的用户认证机制
→ 输出到 docs/specs/feature2-specs/tech/

# 5. 实现代码
/implement 订单管理

# 6. 交付后合并到 baseline
→ baseline/ 现在包含"用户登录"+"订单管理"的完整设计
```

**详细说明**：参见 [docs/specs/README.md](docs/specs/README.md)

---

## 🛠 技术栈

### 后端技术栈

- **语言**：Java 8+
- **框架**：Spring Boot 3.x
- **构建工具**：Maven（多模块架构）
  - `common/`：公共模块
  - `domain/`：领域模型
  - `service/`：业务逻辑
  - `api/`：API接口
  - `infrastructure/`：基础设施
  - `application/`：启动模块
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **ORM**：MyBatis-Plus

### 前端技术栈

- **框架**：Vue 3（Composition API）+ TypeScript
- **构建工具**：Vite
- **UI库**：Element Plus 或 Ant Design Vue
- **状态管理**：Pinia
- **路由**：Vue Router
- **HTTP客户端**：Axios

### 测试技术栈

- **后端测试**：JUnit 5 + Mockito
- **前端测试**：Vitest + Vue Test Utils
- **E2E测试**：Python 3.10+ + Pytest + Playwright
- **接口测试**：Pytest + Requests

### 基础设施

- **容器化**：Docker + Docker Compose
- **版本控制**：Git（多仓库）
- **API规范**：OpenAPI 3.0（Swagger）
- **CI/CD**：GitHub Actions（可选）

---

## 📁 目录结构

### 根目录结构

```
aifd-demo/
├── docs/                        # 文档目录（文档驱动开发）
│   ├── requirements/            # 需求文档
│   │   ├── raw/                 # 原始需求
│   │   └── prd/                 # 产品需求文档
│   ├── designs/                 # 设计文档
│   │   ├── architecture/        # 架构设计
│   │   ├── database/            # 数据库设计
│   │   └── api/                 # API设计（OpenAPI）
│   ├── test-plans/              # 测试计划
│   └── deployment/              # 部署文档
│
├── backend/                     # 后端工程（独立Git仓库）
│   ├── common/                  # 公共模块
│   ├── domain/                  # 领域模块
│   ├── service/                 # 服务模块
│   ├── api/                     # API模块
│   ├── infrastructure/          # 基础设施模块
│   ├── application/             # 启动模块
│   └── pom.xml                  # Maven配置
│
├── frontend/                    # 前端工程（独立Git仓库）
│   ├── src/
│   │   ├── components/          # 组件
│   │   ├── views/               # 页面
│   │   ├── api/                 # API调用
│   │   ├── store/               # 状态管理
│   │   ├── router/              # 路由
│   │   └── types/               # TypeScript类型定义
│   ├── tests/                   # 前端测试
│   ├── package.json
│   └── vite.config.ts
│
├── testing/                        # 测试工程（独立Git仓库）
│   ├── unit/                    # 单元测试
│   ├── integration/             # 集成测试
│   ├── e2e/                     # 端到端测试
│   ├── data/                    # 测试数据
│   └── requirements.txt
│
├── deployment/                  # 本地启动配置
│   ├── docker/                  # Docker配置
│   │   ├── backend/Dockerfile
│   │   ├── frontend/Dockerfile
│   │   └── docker-compose.yml   # 本地开发环境配置
│   └── scripts/                 # 启动脚本
│       ├── start-local.bat      # Windows启动脚本
│       ├── stop-local.bat       # Windows停止脚本
│       └── logs.bat             # Windows查看日志脚本
│
├── knowledge/                   # 知识库
│   ├── templates/               # 模板库
│   ├── best-practices/          # 最佳实践
│   ├── code-snippets/           # 代码片段
│   └── lessons-learned/         # 经验总结
├── workspace/                   # AI工作空间
├── CLAUDE.md                    # 本文档
└── README.md                    # 项目说明
```

### 关键目录说明

#### `docs/` 目录

所有文档的统一存放位置，遵循文档驱动开发：
- **文档命名规范**：`{需求ID}_{文档类型}.md`
- **API文档**：使用 OpenAPI 3.0 格式（YAML）
- **版本管理**：重要文档修改时保留历史版本

#### `knowledge/` 目录

知识库的核心目录，持续学习和积累：
- **持续更新**：每次需求完成后更新
- **版本控制**：重要知识变更要提交到 Git
- **分类管理**：按模板、实践、片段、经验分类

#### 多仓库管理

`backend/`、`frontend/`、`testing/` 各自独立的 Git 仓库：
- 各自独立提交和推送
- 避免代码混淆
- 符合微服务架构理念

---

## 📏 开发规范

### 命名规范

#### 后端命名

```java
// 包名：全小写，单数形式
com.company.project.service
com.company.project.controller

// 类名：大驼峰，名词
public class UserService { }
public class OrderController { }

// 方法名：小驼峰，动词开头
public User getUserById(Long id) { }
public void createOrder(OrderRequest request) { }

// 变量名：小驼峰，有意义
private String userName;
private List<OrderItem> orderItems;

// 常量：全大写，下划线分隔
public static final String DEFAULT_CHARSET = "UTF-8";
public static final int MAX_RETRY_TIMES = 3;
```

#### 前端命名

```typescript
// 组件名：大驼峰
export default defineComponent({
  name: 'UserList'
})

// 文件名：短横线分隔
user-list.vue
user-service.ts

// 变量/函数：小驼峰
const userName = ref('')
const fetchUserList = async () => { }

// 常量：全大写
const MAX_PAGE_SIZE = 100
const API_BASE_URL = '/api'

// 类型/接口：大驼峰
interface User {
  id: number
  name: string
}
```

#### 数据库命名

```sql
-- 表名：小写字母，下划线分隔，使用复数
user_profiles
order_items

-- 字段名：小写字母，下划线分隔
user_name
created_at

-- 索引名：前缀 + 表名 + 字段
idx_user_profiles_email
uk_user_profiles_username
```

### Git提交规范

遵循 Conventional Commits：

**格式**：
```
<type>(<scope>): <subject>
```

**类型（type）**：
- `feat`：新功能
- `fix`：修复bug
- `docs`：文档更新
- `refactor`：重构
- `test`：测试相关

**示例**：
```bash
# 后端
feat(user): add user registration API
fix(order): resolve payment timeout issue

# 前端
feat(login): implement login page
fix(dashboard): fix chart rendering issue

# 测试
test(user): add unit tests for user service
test(e2e): add login flow test
```

### 代码质量规范

#### 后端代码

```java
/**
 * 用户服务实现类
 *
 * @author Claude Code
 * @since 2026-02-13
 */
@Service
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {

    // 1. 常量定义
    private static final Logger log = LoggerFactory.getLogger(UserServiceImpl.class);

    // 2. 依赖注入（使用final + @RequiredArgsConstructor）
    private final UserRepository userRepository;

    // 3. 公共方法
    @Override
    public User getUserById(Long id) {
        // 参数校验
        Assert.notNull(id, "用户ID不能为空");

        // 业务逻辑
        log.debug("查询用户: id={}", id);
        return userRepository.findById(id)
            .orElseThrow(() -> new ResourceNotFoundException("用户不存在"));
    }

    // 4. 私有方法
    private void validateUser(User user) {
        // 验证逻辑
    }
}
```

**要求**：
- 所有公共方法必须有 JavaDoc 注释
- 使用 SLF4J 日志，避免 System.out.println
- 参数必须校验，避免 NullPointerException
- 异常处理完善，使用自定义业务异常

#### 前端代码

```vue
<script setup lang="ts">
// 1. 导入
import { ref, computed, onMounted } from 'vue'
import type { User } from '@/types/user'
import { userApi } from '@/api/user'

// 2. Props定义
interface Props {
  userId: number
  title?: string
}
const props = withDefaults(defineProps<Props>(), {
  title: '默认标题'
})

// 3. Emits定义
interface Emits {
  (e: 'update', user: User): void
}
const emit = defineEmits<Emits>()

// 4. 响应式数据
const user = ref<User | null>(null)
const loading = ref(false)

// 5. 计算属性
const displayName = computed(() => {
  return user.value?.name || '未知用户'
})

// 6. 方法
const fetchUser = async () => {
  loading.value = true
  try {
    const data = await userApi.getUser(props.userId)
    user.value = data
  } catch (error) {
    console.error('获取用户失败', error)
  } finally {
    loading.value = false
  }
}

// 7. 生命周期
onMounted(() => {
  fetchUser()
})
</script>

<template>
  <div class="user-detail">
    <h2>{{ title }}</h2>
    <div v-if="loading">加载中...</div>
    <div v-else-if="user">
      <p>姓名：{{ displayName }}</p>
    </div>
  </div>
</template>

<style scoped>
.user-detail {
  padding: 20px;
}
</style>
```

**要求**：
- 使用 TypeScript，类型定义完整
- 组件必须有 Props 和 Emits 定义
- 避免 console.log 残留（生产环境）
- 样式使用 scoped，避免全局污染

---

## 🔄 工作流程

### 完整开发流程图

```
┌─────────────────────────────────────────────────────────────────┐
│  阶段1: 需求分析                                                 │
│  执行者: ProductAgent                                            │
│  输入: 用户原始需求                                              │
│  输出: PRD文档                                                   │
│  人工检查点: ✓ PRD审核                                           │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段2: 技术设计                                                 │
│  执行者: ArchitectAgent                                          │
│  输入: 已审核的PRD                                               │
│  输出: 架构设计、API契约（OpenAPI 3.0）、数据库设计              │
│  人工检查点: ✓ 技术方案审核                                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段3: 测试用例设计                                             │
│  执行者: TestAgent                                               │
│  输入: PRD + 技术设计文档 + API契约                              │
│  输出: 测试计划 + 测试用例代码                                   │
│  人工检查点: 无（自动执行）                                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段4: 并行开发                                                 │
│  ┌──────────────┐  ┌──────────────┐                             │
│  │BackendAgent  │  │FrontendAgent │                             │
│  │后端代码开发  │  │前端代码开发  │                             │
│  └──────────────┘  └──────────────┘                             │
│  输出: 后端代码 + 前端代码 + 单元测试                            │
│  人工检查点: 无（代码自动提交到feature分支）                    │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段5: 自动化测试                                               │
│  执行者: TestAgent                                               │
│  流程:                                                           │
│    1. 启动测试环境 (Docker Compose)                              │
│    2. 运行单元测试                                               │
│    3. 运行集成测试                                               │
│    4. 运行E2E测试                                                │
│    5. 生成测试报告                                               │
│  自愈机制: 测试失败 → 分析日志 → 自动修复 → 重新测试（最多3次） │
│  人工检查点: 无（测试失败且自愈失败时才通知人工）                │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段6: 本地启动配置生成                                         │
│  执行者: DevOpsAgent                                             │
│  输出: Dockerfile + docker-compose.yml + 启动脚本                │
│  人工检查点: 无（自动执行）                                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段7: 本地启动联调                                             │
│  执行者: DevOpsAgent                                             │
│  流程:                                                           │
│    1. 构建Docker镜像                                             │
│    2. 启动本地服务（Docker Compose）                             │
│    3. 健康检查                                                   │
│    4. 本地联调测试                                               │
│  人工检查点: ✓ 本地启动验证                                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段8: 知识沉淀                                                 │
│  执行者: 所有Agent                                               │
│  流程:                                                           │
│    1. 总结开发经验                                               │
│    2. 提取可复用代码片段                                         │
│    3. 更新最佳实践文档                                           │
│    4. 记录度量数据                                               │
│  人工检查点: 无（自动执行）                                      │
└─────────────────────────────────────────────────────────────────┘
```

### 各阶段详细说明

#### 阶段1: 需求分析

**目标**：将用户的原始需求转化为结构化的产品需求文档（PRD）

**执行步骤**：

1. **需求理解**
   - 分析需求的业务背景和价值
   - 识别目标用户和使用场景
   - 提取功能点和非功能需求

2. **知识检索**
   ```bash
   # 通过 Knowledge MCP 检索历史相似需求
   knowledge.search(query="类似需求", type="prd", top_k=3)
   ```

3. **PRD生成**
   - 使用PRD模板
   - 编写需求背景、目标用户、功能列表
   - 编写用户故事和验收标准
   - 生成原型图建议

4. **输出文档**
   ```bash
   # 保存到 docs/requirements/prd/
   docs/requirements/prd/REQ-001_用户登录功能_PRD.md
   ```

5. **人工审核**
   - 等待人工审核PRD
   - 审核通过 → 进入下一阶段
   - 审核不通过 → 记录意见，重新生成PRD

#### 阶段2: 技术设计

**目标**：基于PRD生成完整的技术设计方案，包括API契约

**执行步骤**：

1. **架构分析**
   - 识别需要新增/修改的模块
   - 设计模块间的调用关系
   - 选择合适的技术方案

2. **数据库设计**
   - 设计ER图
   - 设计表结构（遵循三范式）
   - 设计索引
   - 生成DDL脚本

3. **API设计（核心）**
   ```bash
   # 生成 OpenAPI 3.0 规范文档
   docs/designs/api/REQ-001_api-spec.yaml
   ```

   **API契约示例**：
   ```yaml
   openapi: 3.0.0
   info:
     title: 用户管理API
     version: 1.0.0
   paths:
     /api/users:
       post:
         summary: 创建用户
         requestBody:
           required: true
           content:
             application/json:
               schema:
                 type: object
                 properties:
                   username:
                     type: string
                   email:
                     type: string
         responses:
           '200':
             description: 成功
             content:
               application/json:
                 schema:
                   $ref: '#/components/schemas/User'
   components:
     schemas:
       User:
         type: object
         properties:
           id:
             type: integer
           username:
             type: string
           email:
             type: string
   ```

4. **生成设计文档**
   ```bash
   docs/designs/architecture/REQ-001_architecture.md
   docs/designs/database/REQ-001_database.md
   docs/designs/api/REQ-001_api-spec.yaml  # 核心契约
   ```

5. **人工审核**
   - 审核架构合理性
   - 审核数据库设计
   - 审核API契约
   - 审核通过 → 进入下一阶段

#### 阶段3: 测试用例设计

**目标**：根据需求和设计生成全面的测试用例

**执行步骤**：

1. **测试计划**
   - 单元测试范围
   - 集成测试范围
   - E2E测试场景

2. **用例设计**
   - 正常流程测试
   - 异常流程测试
   - 边界值测试

3. **测试代码生成**
   ```bash
   # 后端测试
   test/unit/backend/test_user_service.py
   test/integration/test_user_api.py

   # 前端测试
   test/unit/frontend/UserLogin.spec.ts

   # E2E测试
   test/e2e/test_user_login_flow.py
   ```

#### 阶段4: 并行代码开发

**目标**：根据技术设计文档和API契约生成后端和前端代码

**后端开发流程（BackendAgent）**：

```bash
# 1. Git操作
cd backend
git checkout develop
git pull origin develop
git checkout -b feature/REQ-001

# 2. 根据 api-spec.yaml 生成代码

# 2.1 生成 Entity
backend/domain/src/main/java/com/company/domain/entity/User.java

# 2.2 生成 Repository
backend/infrastructure/src/main/java/com/company/infrastructure/repository/UserRepository.java

# 2.3 生成 Service
backend/service/src/main/java/com/company/service/UserService.java
backend/service/src/main/java/com/company/service/impl/UserServiceImpl.java

# 2.4 生成 DTO（根据 api-spec.yaml）
backend/domain/src/main/java/com/company/domain/dto/CreateUserRequest.java
backend/domain/src/main/java/com/company/domain/vo/UserVO.java

# 2.5 生成 Controller（根据 api-spec.yaml）
backend/api/src/main/java/com/company/api/controller/UserController.java

# 2.6 生成单元测试
backend/service/src/test/java/com/company/service/UserServiceTest.java

# 3. 代码检查
mvn checkstyle:check
mvn test

# 4. 提交代码
git add .
git commit -m "gen(user): generate user service and controller from API spec"
git push origin feature/REQ-001
```

**前端开发流程（FrontendAgent）**：

```bash
# 1. Git操作
cd frontend
git checkout develop
git pull origin develop
git checkout -b feature/REQ-001

# 2. 根据 api-spec.yaml 生成代码

# 2.1 生成 TypeScript 类型定义（根据 api-spec.yaml）
frontend/src/types/user.ts

# 2.2 生成 API 调用函数（根据 api-spec.yaml）
frontend/src/api/user.ts

# 2.3 生成页面组件
frontend/src/views/user/UserLogin.vue
frontend/src/views/user/UserRegister.vue

# 2.4 生成业务组件
frontend/src/components/user/UserForm.vue

# 2.5 更新路由
frontend/src/router/index.ts

# 2.6 生成单元测试
frontend/src/views/user/__tests__/UserLogin.spec.ts

# 3. 代码检查
npm run lint
npm run type-check
npm run test:unit

# 4. 提交代码
git add .
git commit -m "gen(login): generate user login page from API spec"
git push origin feature/REQ-001
```

#### 阶段5: 自动化测试

**目标**：全面验证代码质量，自动修复问题

**执行步骤**：

```bash
# 1. 环境准备
cd test
docker-compose up -d  # 启动MySQL, Redis等依赖

# 2. 单元测试
# 后端
cd backend
mvn test
mvn jacoco:report  # 生成覆盖率报告

# 前端
cd frontend
npm run test:unit
npm run coverage

# 3. 集成测试
cd test
pytest integration/ -v --html=reports/integration-report.html

# 4. E2E测试
pytest e2e/ -v --html=reports/e2e-report.html

# 5. 生成汇总报告
python scripts/generate_test_report.py
```

**自愈机制**：

```
测试失败
    ↓
解析测试日志，定位失败原因
    ↓
判断是否可自动修复
    ├─ 是 → 修复代码 → 重新运行测试 → 成功 → 进入下一阶段
    │                                 ↓ 失败
    │                          重试次数 < 3?
    │                                 ├─ 是 → 再次修复
    │                                 └─ 否 → 请求人工介入
    └─ 否 → 请求人工介入
```

#### 阶段6: 本地启动配置生成

**目标**：生成本地Windows环境启动所需的配置文件

**执行步骤**：

```bash
# 1. 生成后端Dockerfile
deployment/docker/backend/Dockerfile

# 2. 生成前端Dockerfile
deployment/docker/frontend/Dockerfile

# 3. 生成Docker Compose（本地开发环境）
deployment/docker/docker-compose.yml

# 4. 生成启动脚本
deployment/scripts/start-local.bat     # Windows启动脚本
deployment/scripts/stop-local.bat      # Windows停止脚本
deployment/scripts/logs.bat            # Windows查看日志脚本

# 5. 生成本地配置说明
docs/deployment/local-setup.md

# 注意：无需人工审核，直接进入下一阶段
```

#### 阶段7: 本地启动联调

**目标**：在本地Windows环境启动所有服务并进行联调测试

**执行步骤**：

```bash
# 1. 构建Docker镜像
cd deployment
docker-compose build

# 2. 启动本地服务
docker-compose up -d

# 3. 等待服务启动完成
timeout /t 30

# 4. 健康检查
curl http://localhost:8080/actuator/health   # 后端健康检查
curl http://localhost:3000                    # 前端可访问性检查

# 5. 查看服务状态
docker-compose ps

# 6. 本地联调测试
# 在Windows本地浏览器打开 http://localhost:3000
# 手动验证核心功能是否正常

# 7. 查看日志（如有问题）
docker-compose logs backend
docker-compose logs frontend

# 8. 人工验证
# 等待开发者确认本地联调成功
```

**常用命令**：
```bash
# 停止所有服务
docker-compose down

# 重启某个服务
docker-compose restart backend

# 查看实时日志
docker-compose logs -f backend

# 进入容器调试
docker-compose exec backend bash
```

#### 阶段8: 知识沉淀

**目标**：总结经验，更新知识库

**执行步骤**：

```bash
# 1. 回顾总结
knowledge/lessons-learned/REQ-001-retrospective.md

# 2. 提取代码片段
knowledge/code-snippets/backend/user-registration.java
knowledge/code-snippets/frontend/login-form.vue

# 3. 更新最佳实践
knowledge/best-practices/api-design-principles.md

# 4. 记录度量数据
knowledge/metrics/REQ-001-metrics.json
{
  "requirement_id": "REQ-001",
  "total_days": 7,
  "code_lines": 1500,
  "test_coverage": 85,
  "bug_count": 2,
  "ai_efficiency": 0.85
}
```


## ✅ 质量保证

### 代码质量检查清单

**后端**：
```yaml
- [ ] 代码符合 Java 编码规范
- [ ] 单元测试覆盖率 >= 80%
- [ ] 所有公共方法都有 JavaDoc 注释
- [ ] 没有 TODO 注释遗留
- [ ] 异常处理完善
- [ ] 日志记录充分
- [ ] 参数校验完整
```

**前端**：
```yaml
- [ ] 代码通过 ESLint 检查
- [ ] 类型定义完整（TypeScript）
- [ ] 组件有 Props 和 Emits 定义
- [ ] 单元测试覆盖核心逻辑
- [ ] 无 console.log 残留
- [ ] 样式使用 scoped
```

### 测试质量标准

```yaml
单元测试:
  - 覆盖率: >= 80%
  - 测试用例: 包含正常、异常、边界情况
  - 命名规范: test{方法名}_{场景}

集成测试:
  - 覆盖: 所有API接口
  - 数据隔离: 每个测试独立数据
  - 清理机制: 测试后清理数据

E2E测试:
  - 覆盖: 核心业务流程
  - 截图: 失败时自动截图
```

### 本地启动验证要求

```yaml
启动前检查:
  - [ ] 所有测试通过
  - [ ] Docker镜像构建成功
  - [ ] docker-compose.yml配置正确
  - [ ] 端口未被占用（3306, 6379, 8080, 3000）

启动后验证:
  - [ ] 所有容器状态为Up
  - [ ] 后端健康检查通过 (http://localhost:8080/actuator/health)
  - [ ] 前端可访问 (http://localhost:3000)
  - [ ] 日志无ERROR
  - [ ] 核心功能联调成功
  - [ ] 数据库连接正常
  - [ ] Redis连接正常
```

---

## 🔑 关键说明

### 人工介入点

```yaml
必须人工审核的环节:
  1. PRD审核
     - 审核内容: 需求合理性、功能完整性
     - 位置: 阶段1结束后

  2. 技术方案审核
     - 审核内容: 架构设计、数据库设计、API契约
     - 位置: 阶段2结束后

  3. 本地启动验证
     - 审核内容: 服务启动状态、核心功能联调
     - 位置: 阶段7本地启动后
     - 说明: 开发者在浏览器中验证功能是否正常

可选人工介入的环节:
  1. 代码Review
     - 时机: PR创建后
     - 说明: 可以设置自动合并策略，也可以人工Review

  2. 测试结果Review
     - 时机: 测试失败且自愈失败时
     - 说明: 正常情况自动处理，异常时人工介入
```

### 自愈机制

**触发条件**：测试失败

**执行流程**：
```
1. 测试失败
   ↓
2. 解析测试日志
   - 提取错误信息
   - 定位失败的测试用例
   - 识别失败原因（编译错误、运行时错误、断言失败等）
   ↓
3. 定位问题代码
   - 根据错误堆栈定位文件和行号
   - 分析相关代码逻辑
   ↓
4. 自动修复
   - 修复常见错误（如空指针、类型错误、逻辑错误）
   - 更新相关代码
   ↓
5. 重新运行测试
   - 重新执行失败的测试用例
   ↓
6. 判断结果
   ├─ 成功 → 进入下一阶段
   └─ 失败 → 重试次数 < 3?
              ├─ 是 → 回到步骤2
              └─ 否 → 请求人工介入
```

**自愈限制**：
- 最多重试3次
- 只修复常见错误类型
- 复杂逻辑错误需要人工介入

### 契约驱动流程

**核心思想**：OpenAPI 3.0 作为前后端协作的单一真实来源

**流程**：
```
1. ArchitectAgent 设计API
   ↓
   生成 api-spec.yaml（OpenAPI 3.0格式）
   ↓
2. BackendAgent 根据契约生成代码
   ├─ 从 api-spec.yaml 提取路径和方法 → 生成 Controller
   ├─ 从 api-spec.yaml 提取请求体 → 生成 DTO (Request)
   └─ 从 api-spec.yaml 提取响应体 → 生成 VO (Response)
   ↓
3. FrontendAgent 根据契约生成代码
   ├─ 从 api-spec.yaml 提取路径和方法 → 生成 API 函数
   ├─ 从 api-spec.yaml 提取响应体 → 生成 TypeScript 类型
   └─ 调用 API 函数完成业务逻辑
   ↓
4. TestAgent 根据契约生成测试
   ├─ 从 api-spec.yaml 提取所有端点 → 生成集成测试
   └─ 从 api-spec.yaml 提取请求/响应示例 → 生成测试数据
```

**优势**：
- 前后端开发可以并行
- 接口定义统一，避免沟通偏差
- 自动生成代码，减少手动编写
- 自动生成测试用例，提高测试覆盖率

---


## 🚀 快速开始示例

### 完整示例：实现用户登录功能

**需求输入**：
```
用户需求: 实现用户登录功能，用户通过用户名和密码登录系统，登录成功后返回JWT token
```

**执行流程**：

#### 步骤1：生成PRD

```bash
# ProductAgent 执行
# 输出: docs/requirements/prd/REQ-001_用户登录_PRD.md

内容概要:
- 需求背景: 用户需要登录系统才能访问受保护的资源
- 功能点:
  1. 用户输入用户名和密码
  2. 系统验证用户名和密码
  3. 验证成功返回JWT token
  4. 验证失败返回错误信息
- 验收标准:
  - 登录成功返回token
  - 登录失败返回明确错误提示
  - 密码错误超过5次锁定账户10分钟
```

#### 步骤2：生成技术设计和API契约

```bash
# ArchitectAgent 执行
# 输出:
#   docs/designs/architecture/REQ-001_architecture.md
#   docs/designs/database/REQ-001_database.sql
#   docs/designs/api/REQ-001_api-spec.yaml  # 核心契约
```

**API契约示例**（`REQ-001_api-spec.yaml`）：
```yaml
openapi: 3.0.0
info:
  title: 用户认证API
  version: 1.0.0
paths:
  /api/auth/login:
    post:
      summary: 用户登录
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: "admin"
                password:
                  type: string
                  format: password
                  example: "password123"
      responses:
        '200':
          description: 登录成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                  expiresIn:
                    type: integer
                    example: 86400
        '401':
          description: 用户名或密码错误
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    example: 401
                  message:
                    type: string
                    example: "用户名或密码错误"
```

#### 步骤3：生成测试用例

```bash
# TestAgent 执行
# 输出: test/integration/test_auth_api.py
```

```python
import pytest
import requests

def test_login_success():
    """测试登录成功"""
    response = requests.post('http://localhost:8080/api/auth/login', json={
        'username': 'admin',
        'password': 'password123'
    })

    assert response.status_code == 200
    data = response.json()
    assert 'token' in data
    assert 'expiresIn' in data

def test_login_failure():
    """测试登录失败"""
    response = requests.post('http://localhost:8080/api/auth/login', json={
        'username': 'admin',
        'password': 'wrongpassword'
    })

    assert response.status_code == 401
    data = response.json()
    assert data['code'] == 401
    assert '用户名或密码错误' in data['message']
```

#### 步骤4：并行生成后端和前端代码

**后端代码生成（BackendAgent）**：

根据 `api-spec.yaml` 生成：

1. **DTO（请求）**：
```java
// backend/domain/src/main/java/com/company/domain/dto/LoginRequest.java
@Data
public class LoginRequest {
    @NotBlank(message = "用户名不能为空")
    private String username;

    @NotBlank(message = "密码不能为空")
    private String password;
}
```

2. **VO（响应）**：
```java
// backend/domain/src/main/java/com/company/domain/vo/LoginResponse.java
@Data
@AllArgsConstructor
public class LoginResponse {
    private String token;
    private Integer expiresIn;
}
```

3. **Controller**：
```java
// backend/api/src/main/java/com/company/api/controller/AuthController.java
@RestController
@RequestMapping("/api/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    /**
     * 用户登录
     */
    @PostMapping("/login")
    public ResponseEntity<LoginResponse> login(@Valid @RequestBody LoginRequest request) {
        LoginResponse response = authService.login(request);
        return ResponseEntity.ok(response);
    }
}
```

4. **Service**：
```java
// backend/service/src/main/java/com/company/service/impl/AuthServiceImpl.java
@Service
@RequiredArgsConstructor
public class AuthServiceImpl implements AuthService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    private final JwtTokenProvider jwtTokenProvider;

    @Override
    public LoginResponse login(LoginRequest request) {
        // 查询用户
        User user = userRepository.findByUsername(request.getUsername())
            .orElseThrow(() -> new UnauthorizedException("用户名或密码错误"));

        // 验证密码
        if (!passwordEncoder.matches(request.getPassword(), user.getPassword())) {
            throw new UnauthorizedException("用户名或密码错误");
        }

        // 生成token
        String token = jwtTokenProvider.generateToken(user);

        return new LoginResponse(token, 86400);
    }
}
```

**前端代码生成（FrontendAgent）**：

根据 `api-spec.yaml` 生成：

1. **TypeScript类型定义**：
```typescript
// frontend/src/types/auth.ts
export interface LoginRequest {
  username: string
  password: string
}

export interface LoginResponse {
  token: string
  expiresIn: number
}
```

2. **API函数**：
```typescript
// frontend/src/api/auth.ts
import request from '@/utils/request'
import type { LoginRequest, LoginResponse } from '@/types/auth'

export const authApi = {
  /**
   * 用户登录
   */
  login(data: LoginRequest): Promise<LoginResponse> {
    return request.post('/api/auth/login', data)
  }
}
```

3. **Vue组件**：
```vue
<!-- frontend/src/views/auth/Login.vue -->
<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { authApi } from '@/api/auth'
import type { LoginRequest } from '@/types/auth'

const router = useRouter()
const loginForm = ref<LoginRequest>({
  username: '',
  password: ''
})
const loading = ref(false)

const handleLogin = async () => {
  loading.value = true
  try {
    const response = await authApi.login(loginForm.value)
    localStorage.setItem('token', response.token)
    router.push('/dashboard')
  } catch (error) {
    console.error('登录失败', error)
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <div class="login-page">
    <h1>用户登录</h1>
    <el-form :model="loginForm">
      <el-form-item label="用户名">
        <el-input v-model="loginForm.username" />
      </el-form-item>
      <el-form-item label="密码">
        <el-input v-model="loginForm.password" type="password" />
      </el-form-item>
      <el-form-item>
        <el-button type="primary" :loading="loading" @click="handleLogin">
          登录
        </el-button>
      </el-form-item>
    </el-form>
  </div>
</template>
```

#### 步骤5：自动化测试

```bash
# TestAgent 执行

# 1. 启动测试环境
docker-compose up -d

# 2. 运行单元测试
cd backend && mvn test
cd frontend && npm run test:unit

# 3. 运行集成测试
pytest test/integration/test_auth_api.py -v

# 4. 测试结果
# ✓ 所有测试通过
# ✓ 代码覆盖率: 85%
```

#### 步骤6：生成本地启动配置

```bash
# DevOpsAgent 执行
# 生成本地Windows环境启动配置
```

**Dockerfile（后端）**：
```dockerfile
FROM maven:3.9-eclipse-temurin-8 AS build
WORKDIR /app
COPY pom.xml .
COPY backend backend
RUN mvn clean package -DskipTests

FROM eclipse-temurin:8-jre
WORKDIR /app
COPY --from=build /app/backend/application/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**docker-compose.yml（本地开发）**：
```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mydb
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/mydb
      SPRING_REDIS_HOST: redis
    depends_on:
      - mysql
      - redis

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      VITE_API_BASE_URL: http://localhost:8080
    depends_on:
      - backend

volumes:
  mysql-data:
```

**启动脚本（start-local.bat）**：
```batch
@echo off
echo Starting local development environment...
cd deployment
docker-compose up -d
echo.
echo Waiting for services to start...
timeout /t 30
echo.
echo Services are running!
echo Backend: http://localhost:8080
echo Frontend: http://localhost:3000
echo.
docker-compose ps
```

#### 步骤7：本地启动联调

```bash
# DevOpsAgent 执行

# 1. 构建Docker镜像
cd deployment
docker-compose build
# ✓ Backend image built
# ✓ Frontend image built

# 2. 启动本地服务
docker-compose up -d
# Creating network "deployment_default"
# Creating deployment_mysql_1 ... done
# Creating deployment_redis_1 ... done
# Creating deployment_backend_1 ... done
# Creating deployment_frontend_1 ... done

# 3. 等待服务启动（30秒）
timeout /t 30

# 4. 健康检查
curl http://localhost:8080/actuator/health
# 输出: {"status": "UP"}

# 5. 查看服务状态
docker-compose ps
# 所有服务状态: Up

# 6. 打开浏览器测试
start http://localhost:3000
# 手动验证登录功能

# 7. 本地联调验证
# ✓ 前端页面正常显示
# ✓ 登录功能正常
# ✓ 接口调用成功
```

**常用调试命令**：
```bash
# 查看后端日志
docker-compose logs -f backend

# 查看前端日志
docker-compose logs -f frontend

# 停止所有服务
docker-compose down

# 重启后端服务
docker-compose restart backend
```

#### 步骤8：知识沉淀

```bash
# 所有 Agent 执行

# 1. 总结经验
knowledge/lessons-learned/REQ-001-retrospective.md
- 学到了JWT token的实现方式
- OpenAPI契约驱动开发提高了前后端协作效率
- 自动化测试发现了2个边界值问题

# 2. 提取代码片段
knowledge/code-snippets/backend/jwt-token-generation.java
knowledge/code-snippets/frontend/login-form.vue

# 3. 更新最佳实践
knowledge/best-practices/authentication-best-practices.md

# 4. 记录度量数据
{
  "requirement_id": "REQ-001",
  "total_hours": 4,
  "code_lines": 500,
  "test_coverage": 85,
  "ai_efficiency": 0.9
}
```

---
## 重要说明
### 可复用的能力沉淀很重要
- skill、agent等工具使用过程中如果发现可以优化，要在事前或者事后进行优化
### 知识沉淀很重要
- 不管在哪个阶段哪个环节，也不管是处理什么问题，如果判断是很重要的知识点或者是重要的经验教训，需要更新到知识库中
### skill说明
#### skill创建
- Markdown格式（推荐，现代格式）
  - 文件结构：{skill-name}/SKILL.md
  - 格式：YAML front matter + Markdown内容
  - 示例：系统自带的 product-design/SKILL.md