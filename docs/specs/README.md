# 项目规格文档目录

> 本目录包含项目的全量规格文档（baseline）和增量特性规格文档（feature-specs）

## 📁 目录结构

```
docs/specs/
├── README.md                    # 本文档
├── baseline/                    # 项目全量规格文档（最新完整版本）
│   ├── product/                 # 产品设计
│   │   ├── prd/                 # 产品需求文档
│   │   │   └── prd.md
│   │   ├── user-stories/        # 用户故事
│   │   │   └── user-stories.md
│   │   └── prototypes/          # 原型设计
│   │       └── wireframes.md
│   ├── tech/                    # 技术设计
│   │   ├── architecture.md      # 架构设计
│   │   ├── api-spec.yaml        # API规格（OpenAPI 3.0）
│   │   ├── database.md          # 数据库设计
│   │   └── frontend.md          # 前端设计
│   └── test/                    # 测试规格
│       ├── test-plan.md         # 测试计划
│       └── test-cases.md        # 测试用例
│
└── feature{N}-specs/            # 增量特性规格文档（按特性编号）
    ├── feature1-specs/          # 特性1：用户登录
    │   ├── product/
    │   ├── tech/
    │   └── test/
    ├── feature2-specs/          # 特性2：用户管理
    │   ├── product/
    │   ├── tech/
    │   └── test/
    └── feature3-specs/          # 特性3：订单管理
        ├── product/
        ├── tech/
        └── test/
```

---

## 📚 文档类型说明

### 1. baseline/ - 项目全量规格文档

**定位**：项目的完整、最新的规格文档合集

**内容**：
- 包含所有已实现功能的最新设计文档
- 反映当前系统的完整架构、API、数据库、前端设计
- 是了解项目全貌的唯一真实来源

**使用场景**：
```
✅ 当需要了解过往的需求或设计方案时
✅ 当需要理解系统整体架构时
✅ 当新成员加入需要了解项目全貌时
✅ 当设计新特性需要了解现有功能时
```

**更新时机**：
- ✅ 每个 feature 交付后，将 feature-specs 合并到 baseline
- ✅ baseline 始终保持最新，反映当前系统状态

**示例**：
```bash
# 查看当前系统的完整 API 规格
cat docs/specs/baseline/tech/api-spec.yaml

# 查看当前数据库设计
cat docs/specs/baseline/tech/database.md

# 查看产品需求文档（包含所有已实现功能）
cat docs/specs/baseline/product/prd/prd.md
```

---

### 2. feature{N}-specs/ - 增量特性规格文档

**定位**：单个特性的独立设计文档

**命名规范**：
```
feature{N}-specs/
  └── {N} 是特性编号，从1开始递增

示例：
  feature1-specs/   # 第1个特性
  feature2-specs/   # 第2个特性
  feature10-specs/  # 第10个特性
```

**内容**：
- 包含单个特性的产品设计、技术设计、测试设计
- 只包含该特性的增量部分，不重复 baseline 的内容
- 设计时会引用 baseline，但不修改 baseline

**使用场景**：
```
✅ 当开始设计新特性时
✅ 当需要查看某个特性的设计细节时
✅ 当需要追溯某个功能是何时、如何设计的
```

**目录结构**：
```
feature{N}-specs/
├── product/                     # 产品设计
│   ├── prd/
│   │   └── prd.md              # 该特性的PRD
│   ├── user-stories/
│   │   └── user-stories.md     # 该特性的用户故事
│   └── prototypes/
│       └── wireframes.md       # 该特性的原型
├── tech/                        # 技术设计
│   ├── architecture.md         # 该特性的架构设计（增量部分）
│   ├── api-spec.yaml           # 该特性的API规格（增量部分）
│   ├── database.md             # 该特性的数据库设计（新增表/字段）
│   └── frontend.md             # 该特性的前端设计（新增页面/组件）
└── test/                        # 测试设计
    ├── test-plan.md            # 该特性的测试计划
    └── test-cases.md           # 该特性的测试用例
```

---

## 🔄 工作流程

### 阶段1：新特性启动

```
1. 用户提出新需求
   ↓
2. 创建 feature{N}-specs/ 目录
   ↓
3. 使用 /senior-pm 或 /design-feature 生成 PRD
   → 输出到 docs/specs/feature{N}-specs/product/prd/prd.md
```

### 阶段2：技术设计

```
1. 产品设计审核通过
   ↓
2. 阅读 baseline/ 全量文档，理解现有系统
   - baseline/tech/architecture.md   # 了解现有架构
   - baseline/tech/api-spec.yaml     # 了解现有API
   - baseline/tech/database.md       # 了解现有数据库
   - baseline/tech/frontend.md       # 了解现有前端
   ↓
3. 使用 /tech-design 生成技术设计
   → 基于 baseline 理解，设计增量部分
   → 输出到 docs/specs/feature{N}-specs/tech/
   ↓
4. 技术设计审核通过
```

### 阶段3：代码实现

```
1. 使用 /implement 生成代码
   → 基于 feature{N}-specs/tech/ 的设计
   ↓
2. 自动化测试
   ↓
3. 代码审核通过
```

### 阶段4：合并到 baseline

```
1. Feature 交付完成
   ↓
2. 合并 feature{N}-specs/ 到 baseline/

   合并规则：
   - PRD：合并新功能章节到 baseline/product/prd/prd.md
   - API：合并新接口到 baseline/tech/api-spec.yaml
   - 数据库：合并新表/字段到 baseline/tech/database.md
   - 前端：合并新页面/组件到 baseline/tech/frontend.md
   - 测试：合并测试用例到 baseline/test/test-cases.md
   ↓
3. baseline/ 更新完成，反映最新系统状态
   ↓
4. feature{N}-specs/ 保留作为历史记录
```

---

## 📋 使用示例

### 示例1：设计新特性"用户登录"

```bash
# 步骤1：创建 feature 目录
mkdir -p docs/specs/feature1-specs/{product,tech,test}/{prd,user-stories,prototypes}

# 步骤2：生成 PRD
/senior-pm 用户登录
# → 输出到 docs/specs/feature1-specs/product/prd/prd.md

# 步骤3：阅读 baseline 理解现有系统
cat docs/specs/baseline/tech/api-spec.yaml      # 查看现有 API
cat docs/specs/baseline/tech/database.md        # 查看现有数据库
cat docs/specs/baseline/tech/architecture.md    # 查看现有架构

# 步骤4：生成技术设计
/tech-design 用户登录
# → 基于 baseline 理解，设计增量 API、数据库、前端
# → 输出到 docs/specs/feature1-specs/tech/

# 步骤5：实现代码
/implement 用户登录
# → 基于 feature1-specs/tech/ 生成代码

# 步骤6：交付后合并到 baseline
# 将 feature1-specs/ 的设计合并到 baseline/
# → baseline/ 现在包含"用户登录"功能
```

### 示例2：设计新特性"订单管理"（依赖"用户登录"）

```bash
# 步骤1：创建 feature 目录
mkdir -p docs/specs/feature2-specs/{product,tech,test}

# 步骤2：生成 PRD
/senior-pm 订单管理
# → 输出到 docs/specs/feature2-specs/product/prd/prd.md

# 步骤3：阅读 baseline 理解现有系统（包含已实现的"用户登录"）
cat docs/specs/baseline/tech/api-spec.yaml      # 包含登录 API
cat docs/specs/baseline/tech/database.md        # 包含用户表
# → 设计时可以复用用户认证相关的 API 和数据库表

# 步骤4：生成技术设计（增量设计）
/tech-design 订单管理
# → 设计新增的订单 API、订单表等
# → 复用 baseline 中的用户认证机制
# → 输出到 docs/specs/feature2-specs/tech/

# 步骤5：实现代码
/implement 订单管理

# 步骤6：交付后合并到 baseline
# → baseline/ 现在包含"用户登录"+"订单管理"功能
```

---

## 🔍 查找历史特性

如果需要查看某个功能是何时、如何设计的：

```bash
# 查看所有 feature 列表
ls docs/specs/ | grep feature

# 查看特定 feature 的设计
cat docs/specs/feature1-specs/product/prd/prd.md
cat docs/specs/feature1-specs/tech/api-spec.yaml
```

---

## ✅ 最佳实践

### 1. 设计新特性前，必须先读 baseline

```
✅ 好的实践：
1. 先阅读 baseline/tech/api-spec.yaml，了解现有 API
2. 先阅读 baseline/tech/database.md，了解现有数据库
3. 先阅读 baseline/tech/architecture.md，了解现有架构
4. 基于现有设计，设计增量部分

❌ 不好的实践：
1. 不看 baseline，直接设计新特性
2. 设计时不考虑与现有系统的兼容性
3. 重复设计已有的功能
```

### 2. Feature 交付后，必须合并到 baseline

```
✅ 好的实践：
1. Feature 交付后立即合并到 baseline
2. 合并时保持 baseline 文档的完整性和一致性
3. 合并后验证 baseline 是否准确反映当前系统

❌ 不好的实践：
1. Feature 交付后不合并到 baseline
2. Baseline 长期不更新，与实际系统不符
3. 只保留 feature-specs，删除 baseline
```

### 3. Baseline 是唯一真实来源

```
✅ 好的实践：
- 需要了解系统全貌时，查看 baseline
- 新成员培训时，从 baseline 开始
- 代码与 baseline 保持一致

❌ 不好的实践：
- 同时维护多个"全量"文档
- Baseline 与代码不一致
- 文档分散在多个地方
```

---

## 📌 关键原则

### Baseline 始终是最新的

```
Baseline ≈ 当前系统的完整文档镜像
```

- ✅ Baseline 包含所有已实现功能的最新设计
- ✅ Baseline 反映当前代码库的状态
- ✅ Baseline 是理解系统的唯一入口

### Feature-specs 是增量设计

```
Feature-specs = 单个特性的设计文档
```

- ✅ 每个 feature-specs 只包含该特性的设计
- ✅ 设计时引用 baseline，但不修改 baseline
- ✅ 交付后合并到 baseline

### 工作流是单向的

```
Baseline → 理解 → Feature-specs → 设计 → 实现 → 合并 → Baseline（更新）
```

- ✅ 设计新特性时，从 baseline 读取
- ✅ 交付后，向 baseline 写入
- ❌ 设计时不修改 baseline
- ❌ 多个 feature 不并行修改 baseline

---

## 🚀 快速开始

### 新成员加入项目

```bash
# 1. 阅读 baseline，了解项目全貌
cat docs/specs/baseline/product/prd/prd.md
cat docs/specs/baseline/tech/architecture.md
cat docs/specs/baseline/tech/api-spec.yaml

# 2. 查看最近的 feature，了解开发流程
ls docs/specs/ | grep feature | tail -3
cat docs/specs/feature{N}-specs/product/prd/prd.md
```

### 开始设计新特性

```bash
# 1. 查看现有 feature 编号
ls docs/specs/ | grep feature | sort -V | tail -1
# → 假设最新是 feature5-specs，则新特性编号为 6

# 2. 使用 /create-spec 创建目录
/create-spec 用户权限管理
# → 自动创建 docs/specs/feature6-specs/

# 3. 生成 PRD
/senior-pm 用户权限管理

# 4. 阅读 baseline，理解现有系统
cat docs/specs/baseline/tech/api-spec.yaml
cat docs/specs/baseline/tech/database.md

# 5. 生成技术设计
/tech-design 用户权限管理

# 6. 实现代码
/implement 用户权限管理
```

---

## 📞 常见问题

### Q1: 为什么需要 baseline？

**A**: Baseline 是项目的"完整快照"，避免了：
- ❌ 需要查看多个 feature-specs 才能理解系统
- ❌ 不知道某个功能是否已实现
- ❌ 设计新特性时重复已有功能

### Q2: 为什么不直接修改 baseline？

**A**: 为了保持清晰的设计流程：
- ✅ Feature-specs 是"草稿"，可以反复修改
- ✅ Baseline 是"发布版"，只在 feature 交付后更新
- ✅ 避免多个 feature 并行开发时冲突

### Q3: Feature 交付后，feature-specs 是否删除？

**A**: 不删除，保留作为历史记录：
- ✅ 可以追溯某个功能的设计历史
- ✅ 可以查看功能的演变过程
- ✅ 可以回溯问题

### Q4: 如果 baseline 与代码不一致怎么办？

**A**: 应该立即更新 baseline：
- ✅ Baseline 必须始终反映代码的真实状态
- ✅ 如果代码改动了但 baseline 没更新，应该补上
- ✅ Baseline 是文档的唯一真实来源

---

## 📊 文档状态

### Baseline 状态
```
✅ 已完成：baseline/product/prd/prd.md
✅ 已完成：baseline/tech/architecture.md
✅ 已完成：baseline/tech/api-spec.yaml
✅ 已完成：baseline/tech/database.md
✅ 已完成：baseline/tech/frontend.md
✅ 已完成：baseline/test/test-plan.md
```

### Feature-specs 状态
```
📝 待创建：feature1-specs/（等待第一个特性需求）
```

---

**记住**：
- 🔍 理解系统 → 看 **baseline**
- 🎨 设计新特性 → 创建 **feature-specs**
- ✅ 特性交付 → 合并到 **baseline**
- 🔄 Baseline 始终是**最新的完整文档**
